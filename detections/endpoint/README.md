# Endpoint Detections

Detections focused on Windows and Microsoft Defender for Endpoint telemetry.

Projects are organized by specific attacker behavior or detection use case so the collection can scale without creating generic technology folders.